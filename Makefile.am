#ACLOCAL_AMFLAGS = -I autotools
SUBDIRS	= muscle include src test
pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = libihanclient-0.1.pc
EXTRA_DIST = bootstrap.sh
docdir = ${datarootdir}/doc/${PACKAGE}-${VERSION}/
doc_DATA = README
